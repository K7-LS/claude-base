# Handoff: переработка инсталлятора/лаунчера — этап 0 и серия PR по ревью Codex (закрытие сессии 2026-09-02 ночь, DANIIL-LAPTOP)

## TL;DR
- Владелец дал поручение (б): переработать логику установки одним EXE, убрать костыли, VPN из кода полностью, sing-box оставить. Проведена инвентаризация (факты, не ощущения), написан план на 6 этапов с Done when, план прошёл read-only ревью Codex.
- Закрыто: корень трёх симптомов «диалога нет» (`ArrayList` vs `object[]`, #57/#59), этап 0 VPN (#58, с миграцией двух файлов состояния), реальный таймаут движка (#60), CI-матрица 5.1 (#61), частичный успех (#62), корень скиллов OpenCode (#63), диагностика fail-closed (#64).
- Осталось: дождаться мержей #58, #60–#64; ответить Codex по исполнению; собрать комплект + новую диагностику в папку синка; получить три ответа владельца; затем этап 1+4 одной последовательностью.

## Источник истины (читать первыми, cascade)
- План переработки с фактами инвентаризации, таблицей «код движка → реакция GUI», статусами этапов и разведкой:
  `<проект>/rework-bases/отчёты/2026-09-02-installer-переработка-план.md`
- Ревью Codex дословно: `<проект>/rework-bases/отчёты/2026-09-02-codex-ревью-плана-переработки.md`
- Журнал и статус проекта: `<проект>/rework-bases/Claude/ЖУРНАЛ СЕССИЙ.md`, `STATUS.md` (последние три записи за 2026-09-02).
- Репозиторий: `~/repos/llm-foundation-installer` (main + ветки открытых PR). Движок — `src/foundation.ps1` 0.5.10 в этом же репозитории (версия пинится в манифестах пакетов точным равенством — не трогать).

## Состояние артефактов
### PR в K7-LS/llm-foundation-installer (на момент закрытия)
| PR | Что | Состояние |
|---|---|---|
| #57 | диалог по неизвестным записям не показывался никогда (`ArrayList`) | смержен |
| #59 | счётчик «0 файлов» — тот же корень | смержен |
| #58 | этап 0: VPN убран; миграция `connection.json` и `launcher-routes.json` (VPN→Direct); гард-allowlist; инструкции без VPN | смержен |
| #60 | реальный таймаут запуска движка (параллельное чтение потоков) | смержен |
| #61 | CI-job «5.1» собирает через `powershell.exe` (`K7_TEST_POWERSHELL`) | открыт; CI падал на втором флейке того же модуля (`test_stop_route_restores_proxy_without_killing_appx_client`, код 20) — локально проходит под обеими оболочками, rerun запущен |
| #62 | частичная установка не выглядит как полный успех | смержен |
| #63 | корень `.config/opencode/skills` для диалога коллизии | открыт |
| #64 | диагностика fail-closed, schema 2, SHA EXE, все цели из каталога | смержен |

Мониторы автомержа в новом чате ставить заново — **по состоянию последнего run ветки**, не по `statusCheckRollup` (он показывает FAILURE отменённого run после `update-branch` — ловил ложные «СТОП»): `gh run list --branch <ветка> --limit 1 --json databaseId,status,conclusion`; пока `status != completed` — ждать; `conclusion == failure` → `gh run rerun <id> --failed` (до 4 раз, флейки `test_system_proxy_lease`); иначе `gh pr view N --json mergeStateStatus`: CLEAN → `gh pr merge N --squash --delete-branch`; BEHIND → `gh pr update-branch N`; DIRTY → разрешать в `git worktree` («оба хвоста» тестов).

### Локально
- `~/repos/llm-foundation-installer`: рабочая копия на `feat/remove-vpn-mode`, чистая; мои сегодняшние worktree убраны. В `git worktree list` остаются 11 записей прошлых сессий (`~/repos/.worktrees/foundation-*`, `launch-center-*`, ветки `codex/*`, `fix/*`, `repair/*`) — не мои, не трогать без владельца.
- `~/.claude`: четыре коммита сессии (BOM для скриптов, память проекта в hooks Codex, отказ от junction-раздачи скиллов, классификация скиллов релиза) запушены через `auto-push.ps1`. Параллельные PR делались через `git worktree` — **не переключать ветку под фоновым pytest** (поймано два ложных падения).
- `~/K7-packages/{claude,codex,opencode}` — три принятых пакета (0.1.25 / 0.1.27 / 0.1.14).
- `~/K7-Launcher-Employee-full/` — последний собранный комплект (до #58–#64).
- Папка синка с рабочими ПК: `<Я.Диск>/…/LLM/Разработка/K7-Launcher-Employee` — там комплект от 19:43 (с #53–#56), `ДИАГНОСТИКА.cmd` + `worksite-diagnostics.ps1` старой версии (schema 1), `КАК-ЗАПУСТИТЬ.md`. Отчёты сотрудников — подпапка «Ответ с рабочего ПК» (две станции без админ-прав: обе прошли, одна упёрлась в коллизию `acad-recreation` — закрыто #55).
- Scratchpad этой сессии (патчеры, рендеры) — временный, не нужен.

### Codex (мост)
- Тред `01a05cd0-ad84-7723-920e-f57ae7fc5b83`; 6 ответов; последний — ревью плана. Доставщик `~/.claude/scripts/codex_deliver.py --thread <id> --prompt-file <файл>` (таймаут 2400 с; если Codex думает дольше — ответ брать из rollout `~/.codex/sessions/*<thread>*.jsonl`, `task_complete.last_agent_message`, и журналировать вручную через `interop_journal.py --manual`).
- Codex **не уведомлён** об исполнении ревью — сделать пакет после мержа #58: что принято (таблица выше), что отложено (sing-box многоклиентский — политика владельца; post-install через COMSPEC — после #62; общий bounded runner для 23 `ReadToEnd`; версия продукта в двух местах).

## Открытые вопросы владельцу (не отвечены)
1. Шаги мастера — реальные экраны (тогда «Готово» = центр запуска, снимает вопрос «где Launcher») или один экран без полосы шагов?
2. SOCKS5 среди типов прокси — оставить? (sing-box поддерживает; по умолчанию оставляем.)
3. Sing-box для нескольких клиентов одновременно — общий ref-counted транспорт или честная надпись «маршрут занят другим клиентом»? **Техническая позиция Codex получена (ответ №7):** сейчас безопаснее честное ограничение одного proxied-клиента с объяснением `ROUTE_ALREADY_ACTIVE` и кнопкой «остановить маршрут»; общий транспорт — только через отдельный per-user broker (детали в плане). Наивный ref-count в `ClientLauncher` Codex не санкционирует.

## Что дальше (по порядку)
0. **Первым PR — гонка watchdog системного прокси** (подтверждено кодом, детали в плане, секция «Флейки CI»): `StartWatchdog` не ждёт готовности дочернего процесса, `Acquire` возвращает `ACQUIRED` сразу — на рабочей станции это невосстановленный системный прокси после падения клиента; в CI — флейк `test_owner_crash_is_restored_by_internal_watchdog`, трижды за сутки блокировавший мержи. Фикс: ready-маркер watchdog (по образцу resume-маркера на :850) + тест ждёт его перед kill.
1. Домержить #61, #62, #64 (мониторы по последнему run; конфликты хвостов тестов — «оба блока»). #58, #60, #63 уже в main.
2. Пакет Codex: исполнение ревью + три открытых вопроса (владелец решает, Codex — техническая сторона).
3. **Сделано 2026-09-03 ночью:** комплект собран из main (`9998751`, с #57–#64 кроме #61, который CI-only) и положен в папку синка вместе с новой диагностикой; проверен оттуда через `ДИАГНОСТИКА.cmd`: `status=OK`, 7/7, schema 2, SHA EXE `3490b5e12b64…` совпал с манифестом, три плана. Прежний комплект сохранён в подпапке `_прежний-2026-09-02` (требование ревью). Памятка `КАК-ЗАПУСТИТЬ.md` обновлена (два маршрута, частичный успех, отчёт schema 2). Версия продукта всё ещё 0.4.0 — сборки различать по SHA; подъём до 0.4.1 — отдельный PR.
4. Ревью Codex требует новую версию/identity на каждый изменённый EXE и хранение прежнего комплекта рядом — **версия 0.4.0 прошита в `InstallerApp.cs:25` и `build-gui.ps1:473`** — поднять (0.4.1) до следующей раздачи; единый источник версии — этап 4.
5. Маленькие PR из плана: post-install запуск через `LaunchTargetResolver.Resolve` (после #62); общий `BoundedProcess.Run` — сначала хелпер + 14 мест в `ClientBootstrap` (паттерн и таймауты в плане), затем шесть модулей.
6. Этап 1+4 одной последовательностью (классификация 49 CLI-точек → тестовый host → перенос 155 текстовых ассертов на поведение → удаление test-only поверхности из релизного EXE); этап 3 — сначала controller без UI. Перед каждым этапом — upgrade-canary на одной станции через диагностику.

## Правила, подтверждённые владельцем в этой сессии (память записана)
- Автообновление клиента — не ошибка: запускать как есть, максимум строка «установлена версия X, в комплекте Y». `memory/client-selfupdate-is-not-an-error.md`.
- `.ps1` с кириллицей — только UTF-8 с BOM; проверять на целевой машине (там нет pwsh, нет admin). `memory/windows-shell-encoding-and-target-machine-testing.md`. Гарды стоят в обоих репозиториях.
- Решения по среде Codex — самому Codex; план переработки — «строго зеркально».

## Грабли этой сессии (не повторять)
- Генерация `.cmd`/C#/PS через Python-строки с обратными слешами дважды съедала `\v`, `\7`, `\n` — писать такие файлы через Write как есть.
- `pytest … | tail` возвращает код `tail`: итог читать по строке `N passed/failed`.
- `JavaScriptSerializer` отдаёт массивы как `ArrayList`: после `Deserialize<Dictionary<string, object>>` читать только через `IEnumerable`/`ICollection`.
- `gh pr create` без `--head` падает, если локальная ветка ≠ удалённой.

## Промпт для нового чата

> Привет. Это продолжение сессии «Реворк двух баз: переработка инсталлятора/лаунчера (поручение владельца, вариант б)» с DANIIL-LAPTOP.
>
> Что было сделано: инвентаризация инсталлятора и план на 6 этапов с Done when; план прошёл ревью Codex; по ревью закрыто семь PR (#57–#64) — корень «диалога нет» (ArrayList), этап 0 VPN с миграцией двух файлов состояния, реальный таймаут движка, CI-матрица 5.1, частичный успех, корень скиллов OpenCode, диагностика fail-closed.
>
> Текущее состояние: #57 и #59 в main; #58, #60–#64 открыты и ждут CI (флейк watchdog — перезапускать). Рабочая копия `~/repos/llm-foundation-installer` чистая, на ветке `feat/remove-vpn-mode`. Три ответа владельца не получены (шаги мастера; SOCKS5; sing-box для нескольких клиентов).
>
> Что нужно сделать: (1) домержить #58, #60–#64 мониторами с перезапуском флейка; (2) пакет Codex по исполнению ревью через `codex_deliver.py --thread 01a05cd0-ad84-7723-920e-f57ae7fc5b83`; (3) собрать комплект из main + новая диагностика → папка синка, обновить памятку; (4) дальше по плану — post-install через резолвер, общий BoundedProcess, затем этап 1+4.
>
> Контекст: `~/.claude/session-reports/2026-09-02_installer-rework-stage-b/report.md` (полный, читать cascade: `Grep "^##"` → нужная секция); план — `rework-bases/отчёты/2026-09-02-installer-переработка-план.md`; ревью Codex — рядом.
>
> Перед началом прочитай текущие проектные инструкции и материальный статус (`rework-bases/Claude/STATUS.md`). Agents и skills выбирай по нативным описаниям. Не переключай ветку под фоновым pytest; параллельные PR — через `git worktree`.
