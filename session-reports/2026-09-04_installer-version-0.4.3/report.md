# Handoff: инсталлятор — #73–#75 слиты, комплект 0.4.3 в синке, диагностика ноутбука (2026-09-04, DANIIL-LAPTOP)

## Актуальный handoff — читать первым
- **Состояние.** main = `0f11b80`: #73 (центр запуска в профиль + ярлыки, слит владельцем), #74 (Codex CLI 0.153.0) и #75 (версия 0.4.3) — слиты Claude сам squash с удалением веток (владелец 04.09: «Конечно разрешаю, можешь даже не спрашивать» → память `owner-allows-claude-to-merge-green-prs`). Worktrees #73/#74/#75 убраны. Комплект **0.4.3** собран из main (`release-0.4.3.py build`: manifest 0.4.3, FileVersion 0.4.3.0, SHA EXE `07b2ccfe5836449867453b23c5a8625f88b37cd99aef8e3554055b6beedcda90`, SHA cmd/sing-box против манифеста OK, `--commands-json` exit 0) и выложен в синк (`release-0.4.3.py sync`): 0.4.2 (7 файлов) → `_прежний-2026-09-03-вечер`, памятка `КАК-ЗАПУСТИТЬ.md` с разделом «Что нового в 0.4.3» (поле отчёта — `bundle.manifest_version`).
- **Диагностика из синка (ноутбук, отчёт `диагностика-20260904-103115.json`).** Комплект подтверждён: `manifest_version` 0.4.3, `exe_matches_manifest` true, SHA совпал. Статус **ERROR** из-за одной ошибки: `resolve codex-desktop: ответ не JSON (код 30): Microsoft Store package probe failed`. Доказано, что это среда ноутбука, не комплект: прежний 0.4.2 (`_прежний-2026-09-03-вечер`) даёт ту же ошибку (25 с, код 30), а `Get-AppxPackage -Name OpenAI.Codex` падает сам и в Windows PowerShell 5.1, и в pwsh 7 (ошибка OLE-сервера, ~20–35 с); службы AppXSvc/StateRepository Running, висит `PendingFileRenameOperations` (перезагрузка не делалась 3 дня 20 ч). Вчера 17:01 тот же ноутбук на 0.4.2 давал OK. Лечение — перезагрузка ноутбука (владелец), затем повторить `ДИАГНОСТИКА.cmd`. Диагностика заняла ~8 мин против ~1 мин: первый запуск нового single-file EXE распаковывается, плюс probe Store ждёт таймаут 30 с.
- **`codex-cli` на 0.4.3 до установки — BLOCKED `MANAGED_COMMAND_INTEGRITY_FAILED`** (на 0.4.2 — RESOLVED). Причина: `current.json` управляемой копии хранит `source_sha256` пина 0.146.0-alpha.3.1, lock 0.4.3 ждёт 0.153.0 → `ResolveManagedCommand` бросает, `AcceptSelfUpdatedClient` возвращает null, потому что в lock у `codex-cli` `publisher: null`, `signature_required: false` (проверка подписи для него выключена), хотя `codex.exe` подписан валидно (`OpenAI OpCo, LLC`). Снимается установкой 0.4.3 (обновит Codex до 0.153.0). Побочный эффект на ноутбуке владельца: ярлык «K7 Launch Center» на рабочем столе указывает на EXE в синке, то есть уже открывает 0.4.3 — Codex CLI из него заблокирован до установки. Вопрос владельцу (дизайн): должен ли подъём пина блокировать запуск валидно подписанной прежней версии, или принимать её с обычной строкой «установлена X, в комплекте Y» как для самообновившихся клиентов.
- **Прочее в отчёте (ожидаемо).** `claude` план `ACTIVE_DRIFT: SKILL.md` — локальные правки базы на ноутбуке; `claude-code` RESOLVED с примечанием «установлена 2.1.258, в комплекте 2.1.218» (самообновление); `opencode` READY; `codex` план `BLOCKED_USER_DECISION`, unknown_entries 36 (прежние данные — штатный диалог).
- **Следующие шаги по порядку.** 1) Узкий canary на станции (владелец/сотрудник): `ДИАГНОСТИКА.cmd` → установка через `.cmd` → `ДИАГНОСТИКА.cmd`; проверить ярлык «K7 Launch Center» на рабочем столе и в Пуске, `install_plans[codex].version` 0.153.0, `launch_targets[codex-cli]` RESOLVED, `codex-desktop` RESOLVED (на станции Store был жив 03.09). 2) Ноутбук: перезагрузка → повторить диагностику → установить 0.4.3 → проверить read-only sandbox моста (`codex exec --sandbox read-only`, ждать `START:` без `WindowsApps`) → повторить пакет Codex (конфиг после переноса таблиц, маршрут вместо `web-access` для native `norm-lookup`, языковая строка). 3) Решение владельца по блокировке `codex-cli` при подъёме пина (выше). 4) Точка хешей для диагностики (дизайн в отчёте 2026-09-03). 5) Этап 1 (155 текстовых ассертов → behavior).
- **Промпт для нового чата.** «Продолжение реворка инсталлятора (DANIIL-LAPTOP). Прочитай `rework-bases/Claude/STATUS.md`, верх журнала и отчёт `~/.claude/session-reports/2026-09-04_installer-version-0.4.3/report.md`. Проверь ответ canary 0.4.3 в «Ответ с рабочего ПК» (файл новее 2026-09-04 10:31 с другой станции): ярлык, Codex 0.153.0, codex-cli RESOLVED. Если ноутбук перезагружен — повторить диагностику из синка (ожидается OK по codex-desktop). Дальше по списку шагов отчёта. Зелёные PR сливать самому (squash, delete-branch). Решения, меняющие результат, — через владельца; варианты искать самостоятельно. Не переключать ветку под фоновым pytest; параллельные PR — через git worktree; `K7_OFFICECLI_BINARY_PATH=$HOME/repos/.officecli-cache/officecli.exe`; итог прогона — по строке pytest; скрипты и коммиты — файлом, не heredoc; мониторы CI — только через `gh … --jq`.»

## Команды (проверены 2026-09-03, повторены в сценарии)
```powershell
# сборка комплекта из main (cwd = ~/repos/llm-foundation-installer)
pwsh -NoProfile -File tools\build-edition.ps1 -OutputRoot .work\release\employee-0.4.3 -Edition Employee -DistributionMode Preview -PackageRoot $HOME\K7-packages -ClientSourcesLock .\client-sources.lock.json -RuntimeSourcesLock .\runtime-sources.lock.json -RuntimeArchive .\.work\runtime-cache\sing-box-1.13.14-windows-amd64.zip
# диагностика из синка → отчёт в «Ответ с рабочего ПК»
pwsh -NoProfile -ExecutionPolicy Bypass -File "<синк>\worksite-diagnostics.ps1" -BundleRoot "<синк>"
```
Синк: `…\LLM\Разработка\K7-Launcher-Employee`. В папку докладываются `tools/worksite-diagnostics.ps1` (из main), `ДИАГНОСТИКА.cmd` (рукописный, из прежней папки), `КАК-ЗАПУСТИТЬ.md`.

## Грабли этой сессии
- `sed -i` в Git Bash съедает `\` в замене (`LLM\Реворк` → `LLMРеворк`) — текст с обратными слэшами править через Python.
- Python-вывод кириллицы в Bash-инструменте падает на cp1251 — `sys.stdout.reconfigure(encoding='utf-8')` или `PYTHONIOENCODING=utf-8`.
- Транскрипт прошлой сессии: `AskUserQuestion` не использовался — вопросы владельцу были в тексте последнего сообщения (`type == assistant`, блоки `text`).
- В Git Bash нет `jq` — мониторы CI писать только через `gh … --jq`, иначе цикл молча сыпет `jq: command not found` в stderr и никогда не срабатывает.
