# Handoff: инсталлятор — canary 0.4.3 → фикс A + B → комплект 0.4.4 в синке (2026-09-04, DANIIL-LAPTOP)

## Итог дня (12:55) — читать первым
- main `d66e5ed` = 0.4.4: #76 (junction bin, `-TimeoutSec`, контроль зависания), #78 (файлы установщика Codex в комплекте), #79 (версия). Открыт **#80** (build-edition: `@($null)` у клиентов без bundled_assets; EXE не меняется, сливать по зелёному CI, worktree `.worktrees/build-null-assets`).
- **Комплект 0.4.4 в синке** `…/LLM/Разработка/K7-Launcher-Employee`: EXE SHA `c2670669ffa752c7f5db2cb910d5ae2299421f3c8f94a0e42de221818fc875da`, три файла Codex по SHA манифеста, 0.4.3 → `_прежний-2026-09-04-утро`, памятка с «Что нового в 0.4.4». Диагностика ноутбука `диагностика-20260904-125025.json`: OK, 0.4.4, `codex-desktop` RESOLVED, `codex-cli` BLOCKED до установки (ожидаемо).
- Сквозная офлайн-проверка (`iso5.py`): реальный install.ps1 0.153.0 через обёртку из main + файлы пробной сборки на реплике ноутбука — установлено 0.153.0 без сети, junction перенацелен.
- **Canary 0.4.4, 13:00–13:15.** Ноутбук: установка 13:01 прошла — `current.json` 0.153.0 (source_sha `391f24…` = скрипт 0.153.0), junction `current` → релиз 0.153.0, база Codex с backup `backups/codex/20260904T100146Z-…`, копия центра запуска `~/.llm-foundation/launcher` (EXE, cmd, manifest, sing-box — без файлов Codex), ярлыки Desktop и Пуск → копия; диагностика `диагностика-20260904-131455.json`: OK, 0.4.4, все 7 целей RESOLVED. Станция `R-090226727A`: отчёты 13:10:42 и 13:11:27 — bundle 0.4.3 (SHA `07b2ccfe…`), установки не было — Яндекс.Диск не донёс 0.4.4 (+210 МБ) к моменту запуска; повторить, когда окно диагностики покажет «Employee Preview v0.4.4».
- **Мост, read-only sandbox (13:17).** На Codex 0.153.0 `codex exec --sandbox read-only --skip-git-repo-check -C ~/repos "…echo START: && hostname…"` вернул `START:/DANIIL-LAPTOP/END.` (exit 0, 74 с, 26.5k токенов) — ограничение «shell только через danger-full-access» снято. В выводе хуки базы Codex `PostToolUse`/`Stop` печатают «Failed» — пункт в повтор пакета Codex.
- **Открытый вопрос дизайна.** Копия центра запуска в профиле не несёт `bundled_assets`; установка/обновление Codex из неё (ярлык) пойдёт через сеть (файлы отсутствуют → штатный сетевой путь, не ошибка). Варианты: копировать файлы Codex в профиль (+130 МБ на пользователя) либо оставить установки за папкой синка.
- **Было: ждёт canary 0.4.4** (владелец): станция и ноутбук — `ДИАГНОСТИКА.cmd` → `.cmd` → «Проверить план и установить» → `ДИАГНОСТИКА.cmd`. Ожидание: Codex ставится из комплекта без скачивания, `install_plans[codex]` 0.153.0, `codex-cli` RESOLVED, ярлык «K7 Launch Center». Если снова ошибка — текст окна.
- Сценарий выкладки на будущее: `release-next.py` (рядом), пример: `build --version 0.4.5 --expect-pr '#NN'` → `sync --version 0.4.5 --prev-version 0.4.4 --prev-name _прежний-<дата> --whats-new whats-new-0.4.5.md --header-note '…'`; кеш файлов Codex — `.work/client-assets/codex-cli/0.153.0` (команда заполнения в README).

## Актуальный handoff — читать первым
- **Состояние.** main = `0f11b80`: #73 (центр запуска в профиль + ярлыки, слит владельцем), #74 (Codex CLI 0.153.0) и #75 (версия 0.4.3) — слиты Claude сам squash с удалением веток (владелец 04.09: «Конечно разрешаю, можешь даже не спрашивать» → память `owner-allows-claude-to-merge-green-prs`). Worktrees #73/#74/#75 убраны. Комплект **0.4.3** собран из main (`release-0.4.3.py build`: manifest 0.4.3, FileVersion 0.4.3.0, SHA EXE `07b2ccfe5836449867453b23c5a8625f88b37cd99aef8e3554055b6beedcda90`, SHA cmd/sing-box против манифеста OK, `--commands-json` exit 0) и выложен в синк (`release-0.4.3.py sync`): 0.4.2 (7 файлов) → `_прежний-2026-09-03-вечер`, памятка `КАК-ЗАПУСТИТЬ.md` с разделом «Что нового в 0.4.3» (поле отчёта — `bundle.manifest_version`).
- **Диагностика из синка (ноутбук, отчёт `диагностика-20260904-103115.json`).** Комплект подтверждён: `manifest_version` 0.4.3, `exe_matches_manifest` true, SHA совпал. Статус **ERROR** из-за одной ошибки: `resolve codex-desktop: ответ не JSON (код 30): Microsoft Store package probe failed`. Доказано, что это среда ноутбука, не комплект: прежний 0.4.2 (`_прежний-2026-09-03-вечер`) даёт ту же ошибку (25 с, код 30), а `Get-AppxPackage -Name OpenAI.Codex` падает сам и в Windows PowerShell 5.1, и в pwsh 7 (ошибка OLE-сервера, ~20–35 с); службы AppXSvc/StateRepository Running, висит `PendingFileRenameOperations` (перезагрузка не делалась 3 дня 20 ч). Вчера 17:01 тот же ноутбук на 0.4.2 давал OK. Лечение — перезагрузка ноутбука (владелец), затем повторить `ДИАГНОСТИКА.cmd`. Диагностика заняла ~8 мин против ~1 мин: первый запуск нового single-file EXE распаковывается, плюс probe Store ждёт таймаут 30 с.
- **`codex-cli` на 0.4.3 до установки — BLOCKED `MANAGED_COMMAND_INTEGRITY_FAILED`** (на 0.4.2 — RESOLVED). Причина: `current.json` управляемой копии хранит `source_sha256` пина 0.146.0-alpha.3.1, lock 0.4.3 ждёт 0.153.0 → `ResolveManagedCommand` бросает, `AcceptSelfUpdatedClient` возвращает null, потому что в lock у `codex-cli` `publisher: null`, `signature_required: false` (проверка подписи для него выключена), хотя `codex.exe` подписан валидно (`OpenAI OpCo, LLC`). Снимается установкой 0.4.3 (обновит Codex до 0.153.0). Побочный эффект на ноутбуке владельца: ярлык «K7 Launch Center» на рабочем столе указывает на EXE в синке, то есть уже открывает 0.4.3 — Codex CLI из него заблокирован до установки. Вопрос владельцу (дизайн): должен ли подъём пина блокировать запуск валидно подписанной прежней версии, или принимать её с обычной строкой «установлена X, в комплекте Y» как для самообновившихся клиентов.
- **Прочее в отчёте (ожидаемо).** `claude` план `ACTIVE_DRIFT: SKILL.md` — локальные правки базы на ноутбуке; `claude-code` RESOLVED с примечанием «установлена 2.1.258, в комплекте 2.1.218» (самообновление); `opencode` READY; `codex` план `BLOCKED_USER_DECISION`, unknown_entries 36 (прежние данные — штатный диалог).
- **Следующие шаги по порядку (до canary; итог canary — в разделе ниже).** 1) Узкий canary на станции (владелец/сотрудник): `ДИАГНОСТИКА.cmd` → установка через `.cmd` → `ДИАГНОСТИКА.cmd`; проверить ярлык «K7 Launch Center» на рабочем столе и в Пуске, `install_plans[codex].version` 0.153.0, `launch_targets[codex-cli]` RESOLVED, `codex-desktop` RESOLVED (на станции Store был жив 03.09). 2) Ноутбук: перезагрузка → повторить диагностику → установить 0.4.3 → проверить read-only sandbox моста (`codex exec --sandbox read-only`, ждать `START:` без `WindowsApps`) → повторить пакет Codex (конфиг после переноса таблиц, маршрут вместо `web-access` для native `norm-lookup`, языковая строка). 3) Решение владельца по блокировке `codex-cli` при подъёме пина (выше). 4) Точка хешей для диагностики (дизайн в отчёте 2026-09-03). 5) Этап 1 (155 текстовых ассертов → behavior).
- **Промпт для нового чата.** «Продолжение реворка инсталлятора (DANIIL-LAPTOP). Прочитай `rework-bases/Claude/STATUS.md`, верх журнала и отчёт `~/.claude/session-reports/2026-09-04_installer-version-0.4.3/report.md`. Проверь ответ canary 0.4.3 в «Ответ с рабочего ПК» (файл новее 2026-09-04 10:31 с другой станции): ярлык, Codex 0.153.0, codex-cli RESOLVED. Если ноутбук перезагружен — повторить диагностику из синка (ожидается OK по codex-desktop). Дальше по списку шагов отчёта. Зелёные PR сливать самому (squash, delete-branch). Решения, меняющие результат, — через владельца; варианты искать самостоятельно. Не переключать ветку под фоновым pytest; параллельные PR — через git worktree; `K7_OFFICECLI_BINARY_PATH=$HOME/repos/.officecli-cache/officecli.exe`; итог прогона — по строке pytest; скрипты и коммиты — файлом, не heredoc; мониторы CI — только через `gh … --jq`.»

## Canary 0.4.3 (04.09, 10:55–10:59): установка не выполнилась ни на станции, ни на ноутбуке
- **Отчёты.** Станция `R-090226727A`: 10:55:55 (до) и 10:58:21 (после) — идентичны: OK, комплект 0.4.3 подтверждён, `codex-cli` BLOCKED `MANAGED_COMMAND_INTEGRITY_FAILED`, `claude-code` BLOCKED `MANAGED_COMMAND_NOT_FOUND`, план claude `INVALID_PACKAGE` (collision `acad-recreation`), `codex-desktop` RESOLVED. Ноутбук: 10:58:16 и 10:59:28 — идентичны: OK (после перезагрузки Store ожил, `codex-desktop` RESOLVED), `codex-cli` BLOCKED, claude `ACTIVE_DRIFT`. Между «до» и «после» на обеих машинах ничего не изменилось; на ноутбуке нет `~/.llm-foundation/launcher`, ярлык на рабочем столе прежний (03.09 17:17, на EXE в синке), managed Codex по-прежнему 0.146 (`current.json`).
- **Следы на ноутбуке.** 10:53–10:54 центр запуска после перезагрузки (events.log: claude `SKIPPED_OFFLINE release-list-failed`); 10:58:42–10:58:55 скачан codex-base 0.1.27 в `cache/bases`; 10:59:02 `staging/clients/codex-cli/0.153.0/install.ps1`; 10:59:06 `connection.json` (mode Direct); 10:59:28 диагностика «после». Лога установки продукт на диск не пишет (только статус в окне); в журнале событий Windows и CrashDumps ничего; AST-верификация скрипта 0.153.0 проходит (нет `Invoke-Expression`).
- **Что показала изоляция (scratchpad `codex-iso*`).** Официальный `install.ps1` 0.153.0 — новый контракт: `Invoke-WebRequest … -TimeoutSec` (обёртка продукта `BuildOfficialScriptWrapper` объявляет только `-Uri/-OutFile/-UseBasicParsing` → ParameterBindingException → скрипт считает releases.openai.com недоступным и уходит на GitHub), `Invoke-RestMethod` к api.github.com (обёрнут — работает), `CODEX_INSTALL_DIR` уважается (visible bin = junction на `codex-home/packages/standalone/current/bin`, на ноутбуке bin уже junction с 31.08). Через обёртку продукта прогон доходит до «Downloading Codex CLI» и упирается в скачивание: пакет `codex-package-x86_64-pc-windows-msvc.tar.gz` — **129 МБ**, канал ноутбука 200–290 КБ/с (замер curl, обе площадки отвечают 200), то есть 8–11 мин против `--max-time 300` в обёртке и `BoundedProcess 600000` → установка Codex на таком канале не может завершиться. Установленный 0.146 (403 МБ распакованных) ставился 31.08 тем же путём — тогда канал был быстрее.
- **Полный прогон через обёртку (`codex-iso3`, 11:21→11:32).** Скачивание 129 МБ в этот раз уложилось в лимит (соло-канал быстрее замера, который шёл параллельно с другими загрузками), релиз распакован в `codex-home/packages/standalone/releases/0.153.0-…`, а упал скрипт на последнем шаге: `Refusing to replace non-empty directory at …\clients\codex-cliin with a junction` (install.ps1:683) — в реплике `bin` был обычной папкой с codex.exe. На ноутбуке `bin` уже junction (с 31.08, official-script 0.146), на станции — тот же путь установки (0.4.0, 03.09) → этот отказ им, вероятно, не грозит; он грозит машинам, где `bin` — реальная папка. Точная реплика раскладки ноутбука (junction `bin` → `current/bin`) — `codex-iso4`, итог в следующем пункте.
- **Скрин владельца (подтверждено).** Окно: «Ни одна база не готова к установке. Codex: клиент не установлен — Client staging path contains a reparse point». Источник — `InstallOfficialPowerShellScript` → `EnsureSafeDirectory(binRoot)` → `AssertNoReparseAncestors`: `bin` Codex — junction, который создал сам официальный скрипт (standalone-раскладка) при первой установке; первая установка проходит (bin пустая папка), любое обновление — нет. Проявилось на подъёме пина (#74). Пакет 129 МБ и лимит 300 с — второй, независимый дефект. Реплика раскладки ноутбука (`codex-iso4`) упёрлась в артефакт короткого пути `1678~1` (скрипт сравнивает цель junction с префиксом построчно); на реальном ноутбуке цель `current` начинается с `codex-home\packages\standalone` — скрипт перенацелит сам.
- **Решение владельца: A + B.** PR **#76** `fix/codex-cli-upgrade-over-junction` (слит squash `6d46b1e`, worktree убран): junction bin допускается, если реальная цель (`GetFinalPathNameByHandle`) внутри `codex-home`; обёртка принимает `-TimeoutSec`; вместо `--max-time 300` — предел скрипта либо `--speed-limit 1024 --speed-time 60` с потолком 1800 с (и для стейджинга обычных клиентов); bound официального установщика 40 мин, стейджинга 1810 с. Тесты RED→GREEN (5 official + 8 соседних passed). B — **PR #78** `feat/codex-bundled-package` (worktree `.worktrees/codex-bundled`; #76 слит `6d46b1e`, #77 закрыт GitHub при удалении базовой ветки — PR на удалённую базу не перенацеливается, а закрывается; ветка перебазирована на main; 13 целевых тестов passed; полный `test_gui`+`test_editions` локально — 151 passed, 3 skipped, 8:45). **PR #79** — версия 0.4.4 (открыт заранее, FileVersion 2 passed локально), сливать после #78. Сборка 0.4.4 — `release-next.py build --version 0.4.4 --expect-pr '#78' --expect-pr '#79'`, затем `sync --version 0.4.4 --prev-version 0.4.3 --prev-name _прежний-2026-09-04-утро --whats-new whats-new-0.4.4.md`: lock `bundled_assets` для codex-cli (`codex-package-x86_64-pc-windows-msvc.tar.gz` sha `dba6a113…`, `codex-package_SHA256SUMS`, `release.json` 0.153.0), build-edition кладёт файлы рядом с EXE и пишет в манифест, обёртка отдаёт их по URL вместо curl, несовпадение SHA — fail-closed. Детали #77: `bundled_assets` валидируются при загрузке lock (только official-script, плоские имена, одобренные хосты — добавлен `releases.openai.com` в `OfficialHosts`, без него реальный lock не грузился, поймал тест детерминизма); перед запуском скрипта `ResolveBundledAssets` сверяет SHA-256 и размер, карта URL→файл уходит в обёртку через `LLM_BUNDLED_ASSET_MAP`, обёртка отдаёт файл (`Copy-Item` в `-OutFile` или `Content`); `build-edition.ps1 -ClientAssetRoot <root>` берёт `<root>\<client id>\<version>\<file>`, вне Preview файлы обязательны; манифест `client_assets`; фикстуры `tests/fixtures/codex-cli/0.153.0` (release.json, SHA256SUMS). Кеш для сборки: `~/repos/llm-foundation-installer/.work/client-assets/codex-cli/0.153.0` (пакет скачан, SHA `dba6a113…` совпал).
- **Сквозная проверка B (`iso5.py`, 12:30).** Реальный `install.ps1` 0.153.0 через обёртку продукта из main (извлечена из C#), файлы из пробной сборки `-ClientAssetRoot` (`.work/release/employee-dryrun`: манифест `client_assets` с тремя записями), реплика раскладки ноутбука по длинному пути (`~/repos/.tmp-codex-iso5`, junction `bin` → `current/bin`, релиз 0.146), curl подменён заглушкой: результат «Codex CLI 0.153.0 installed successfully», `codex.exe --version` через junction — 0.153.0, `current` перенацелен на релиз 0.153.0, сетевых вызовов не было. Побочное: официальный скрипт дописывает видимый bin в пользовательский PATH (существующее поведение с 0.146); запись реплики из PATH удалена, реплика стёрта.
- **Снято.** Вопрос «ждал ли конца» больше не важен: отказ мгновенный, до запуска скрипта.
- **Варианты исправления (решение владельца).** A) обёртка принимает `-TimeoutSec` и передаёт в curl `--max-time`; вместо фиксированных 300 с — контроль зависания (`--speed-limit/--speed-time`) и потолок ~30 мин; бound официального установщика 600 с → ~40 мин; в окне текст «скачивание ~130 МБ». B) класть пакет Codex (tar.gz + SHA256SUMS) в комплект и отдавать его обёрткой вместо сети — офлайн-установка, +129 МБ к комплекту. C) оставить и требовать канал ≥ 0.5 МБ/с. Рекомендация: A сейчас, B — отдельным решением.

## Команды (проверены 2026-09-03, повторены в сценарии)
```powershell
# сборка комплекта из main (cwd = ~/repos/llm-foundation-installer)
pwsh -NoProfile -File tools\build-edition.ps1 -OutputRoot .work\release\employee-0.4.3 -Edition Employee -DistributionMode Preview -PackageRoot $HOME\K7-packages -ClientSourcesLock .\client-sources.lock.json -RuntimeSourcesLock .\runtime-sources.lock.json -RuntimeArchive .\.work\runtime-cache\sing-box-1.13.14-windows-amd64.zip
# диагностика из синка → отчёт в «Ответ с рабочего ПК»
pwsh -NoProfile -ExecutionPolicy Bypass -File "<синк>\worksite-diagnostics.ps1" -BundleRoot "<синк>"
```
Синк: `…\LLM\Разработка\K7-Launcher-Employee`. В папку докладываются `tools/worksite-diagnostics.ps1` (из main), `ДИАГНОСТИКА.cmd` (рукописный, из прежней папки), `КАК-ЗАПУСТИТЬ.md`.

## Грабли этой сессии
- PowerShell: имена переменных регистронезависимы — `$release = …` затирает параметр `$Release` (час на ложный след «метаданные не совпали»).
- Патчи Python в CRLF-файлах репо (`.cs`, `.py`, `.md` — w/crlf): читать/писать в текстовом режиме или нормализовать `

`; `.ps1` — LF с BOM.
- В PowerShell-вставку обёртки (`-Command` через `QuoteArgument`) не класть двойные кавычки — разделители через `[char]10`/`[char]9`.
- `sed -i` в Git Bash съедает `\` в замене (`LLM\Реворк` → `LLMРеворк`) — текст с обратными слэшами править через Python.
- Python-вывод кириллицы в Bash-инструменте падает на cp1251 — `sys.stdout.reconfigure(encoding='utf-8')` или `PYTHONIOENCODING=utf-8`.
- Транскрипт прошлой сессии: `AskUserQuestion` не использовался — вопросы владельцу были в тексте последнего сообщения (`type == assistant`, блоки `text`).
- В Git Bash нет `jq` — мониторы CI писать только через `gh … --jq`, иначе цикл молча сыпет `jq: command not found` в stderr и никогда не срабатывает.
