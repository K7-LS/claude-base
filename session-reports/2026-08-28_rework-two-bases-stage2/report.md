# Реворк двух баз: мост, диагностика, этап 2 — сводный отчёт сессии

Даты: 2026-08-26 … 2026-08-28. Устройство: ноутбук владельца (developer).
Ядро проекта (STATUS, ЖУРНАЛ СЕССИЙ, отчёты) — папка проекта «Реворк двух
баз»; для Codex и скриптов используется ASCII-junction `%USERPROFILE%\rework-bases`.
Этот отчёт — обезличенный указатель для handoff; первоисточник — ядро проекта.

## Арка сессии (что сделано, по порядку)

1. **Мост Claude↔Codex**: проверен в обе стороны; журнал переговоров
   `~/.claude/interop/dialog.md` + живой дашборд :7343 (переписка, модели,
   ошибки, вкладка «сессии Codex» с полным разбором rollout); скилл
   `codex-bridge`; автостарт дашборда SessionStart-хуком.
2. **Надёжность моста** — 7 инцидентов, все с доказанными причинами и
   защитами (главные: deadlock «мост-в-мосте» → `mcp_servers.claude.enabled=false`
   в вызовах; 30-мин idle-таймаут → per-server timeout 2 ч + правило
   восстановления ответа из rollout). Отчёт: `отчёты/2026-08-26-надёжность-моста.md`.
3. **Тотальная ревизия базы** (4 разведчика) → `session-reports/2026-08-26_total-revision/`.
4. **Д1–Д6 диагностика деградации** (проект): из 48 железных правил Claude
   видел 4; причина — auto-sync снёс @import ядра. Отчёты в `отчёты/`.
5. **Пакет решений исполнен**: @import ядра возвращён (теперь и в релизном
   шаблоне — PR #27), слой Codex дополнен его 4 правилами, updater unmuted
   → затем снят с SessionStart (решение Codex), обезличивание-гард жив.
6. **Вопросы посреди задачи в Desktop**: флаг `default_mode_request_user_input`
   проверен владельцем вживую; раскатка всем через `codex-layer/feature-flags.json`
   + `ensure_feature_flags()` (тесты 3/3).
7. **Волна Б плагинов**: три фокусных плагина 0.2.0 (BIM 4 / construction 6 /
   project-controls 5) из канона через `scripts/build_codex_plugins.py`;
   cutover junction→plugin принят приёмкой Codex 4/4 PASS.
8. **Хаб-задачи**: Codex-native профили агентов (agent-instructions),
   переносимый whitelist (`~/.codex/base/mcp-whitelist.json`, зеркалится
   синком), read-only health-check `scripts/codex_health.py` (7/7 PASS).
9. **Этап 2, sync-base разблокирован** (карта блокеров:
   `session-reports` проекта + PR K7-LS/claude-base-v2#28, MERGED):
   unknown-записи → local exceptions (не удаляются; plan BLOCKED→READY
   проверен на живом релизе), gh снят (анонимный REST+curl), check-release
   уведомляет без установленного слоя (обёртка `scripts/check-release-wrapper.ps1`),
   мина control-skills удалена, containment-списки движка.
10. **Регион**: слой соответствия правилам Anthropic (policy-audit,
    поля аттестации) НЕ удалён — отказ Claude, подтверждён дважды.
    Кодом не исполняется, цели «база любому аккаунту» не мешает.

## Состояние артефактов

- `K7-LS/claude-base-v2` main: PR #27 (ядро в релизном шаблоне) и PR #28
  (sync-base без блокеров) смержены. **Релиз 0.1.24 ещё не собран.**
- Старый consumer-репо: живой скилл sync-base с фиксами, обёртка
  уведомлений, дашборд, журнал — всё в main (коммиты 2026-08-26…28).
- Проект: STATUS и ЖУРНАЛ актуальны на 2026-08-28.

## Открытые хвосты (для нового чата)

1. **Релиз 0.1.24** из main хаба (владелец/хаб-процедура release_builder →
   promotion) — унесёт всё установленным слоем; после релиза — smoke
   `$sync-base` на этой машине (unknown `codex-bridge` должен сохраниться
   как исключение) и обновить `desired-state` на будущее.
2. **Этап 2а: Launcher/Installer** — оптимизация/объединение (репо
   `~/repos/llm-foundation-installer`, PR #27–35 там уже сделаны; точечные
   правки уместны). Разведданные — в отчёте «блокеры sync-base», §5.
3. **Приёмка обновлённого контура Codex'ом** через мост (правила моста —
   скилл `codex-bridge`: фоновый агент, full-access для файловой работы,
   без mcp__claude__*, junction-пути).
4. Общие инструменты «ядро проекта» / «карта понимания» (согласовать с
   Codex зеркально).
5. Этап 3 (СТРОГО после этапа 2): глобальный аудит плагинов мира; найти
   плагин-улучшатель промптов, который сработал у владельца.

## Правила, добытые кровью (не потерять)

- Решения по среде Codex — самому Codex пакетом через мост; длинные пакеты
  — фоновым агентом; при обрыве читать rollout, не перезапускать вслепую.
- Кириллические пути и node_repl Codex несовместимы — только junction.
- WinPS 5.1 требует BOM у UTF-8 .ps1 с кириллицей; StrictMode требует
  `@()` вокруг пайпов перед `.Count`.
- main хаба защищён — только PR; CI-тесты хранят контракты поведения:
  меняешь поведение — меняй тест осознанно.
- settings.json хуки strictly-shared: править settings.shared.json.
